# Build artifacts
target/
*.wasm
artifacts/

# Devnet generated state
devnet/deploy.env
tmpdir/

# Local diagnostic scripts (leading-underscore = scratch, not for upstream)
devnet/scripts/_*
# Local diagnostic helpers at repo root (leading-dot = hidden / scratch)
/.diag-*.sh
/.check-and-deploy.sh
/.fresh-devnet.sh
/.run-precompile-build.sh
/.wait-rpc.sh

# wasmvm-fork patch backups + rebase scratch
wasmvm-fork/patches/*.bak
wasmvm-fork/patches/*.err

# Downloaded toolchains (don't commit large binaries)
go*.tar.gz

# Dependencies
node_modules/

# Environment / secrets
.env
deployed.json
parliament-state.json

# Bud-seal keys and secrets — NEVER commit
*.key
*.pub
*.sealed
*-secrets.toml
!secrets-template.toml

# Pre-publication security artefacts — local-only until formal disclosure
# (caveat 3 from the v0.x.y-security-2 post-release review: draft advisories
# describe exploit narratives in detail; keep off `main` until GHSAs publish)
docs/GHSA_DRAFT.md
docs/GHSA_DRAFT_*.md

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Rust
**/*.rs.bk

# Lock files (component lib)
wavs/Cargo-component.lock

# Package lock (reproducible via npm install)
package-lock.json

# Frontend build
frontend/dist/

# MCP build
mcp/dist/

# Session memory / internal notes — not for public repo
memory/

# Private DM drafts and outreach — contains personal comms
docs/JAKE_DM_*.md
docs/HIGHLANDER_OUTREACH_DM.md
docs/CMW_ISSUE*_PASTE.md
docs/PR924_COMMENT_DRAFT.md
docs/GITHUB_2FA_STEPWISE_GUIDE.md

# Personal files — never commit
# NOTE: `_private/` contains personal/job-application drafts and must stay local.
_private/
cv_*.html
cv_*.pdf
*resume*.md
*resume*.txt
*resume*.pdf
*cover*letter*.md
*cover*letter*.txt
*cover*letter*.pdf
*supporting*statement*.md
*supporting*statement*.txt
*job*application*.md
*job*application*.txt
*APPLICATION.md
docs/ANAESTHESIA_APPLICATION.md
cv_*.txt
*_for_pdf.html
html_to_pdf.py
make_pdf.py
