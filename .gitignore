# Build artifacts
target/
*.wasm

# Dependencies
node_modules/

# Environment / secrets
.env
deployed.json
parliament-state.json

# Bud-seal keys and secrets — NEVER commit
*.key
*.pub
*.sealed
*-secrets.toml
!secrets-template.toml

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Rust
**/*.rs.bk

# Lock files (component lib)
wavs/Cargo-component.lock

# Package lock (reproducible via npm install)
package-lock.json

# Frontend build
frontend/dist/

# MCP build
mcp/dist/

# Personal files — never commit
# NOTE: `_private/` contains personal/job-application drafts and must stay local.
_private/
cv_*.html
cv_*.pdf
*resume*.md
*resume*.txt
*resume*.pdf
*cover*letter*.md
*cover*letter*.txt
*cover*letter*.pdf
*supporting*statement*.md
*supporting*statement*.txt
*job*application*.md
*job*application*.txt
cv_*.txt
*_for_pdf.html
html_to_pdf.py
make_pdf.py
