JunoClaw progress log
=====================

Format: newest first. Pure text. Update at end of each session.
Live status line below; everything else is dated entries.

Last update: 2026-06-28 UTC+1
Status: junod init NATIVE hybrid consensus key flag COMPLETE (--aegis-hybrid-consensus on junod init now generates the Ed25519+ML-DSA-44 validator key directly; previously only the standalone aegis-bench/cmd/hybrid-consensus-keygen tool could). cosmos-sdk fork aegis-phase-d3-hybrid@5792792 and cometbft fork aegis-phase-cf-hybrid@ff4dcefdc083 pushed; SDK go.mod cometbft pin bumped to ff4dcefdc083. Earlier-COMPLETE: D3 account wiring; F6 MsgRotateConsensusKey proto/server/CLI/tests + 24h rate-limit; F7 hybrid remote-signer passthrough; IBC Phase G wiring; CI Go-1.24 bumps across forks; ARM64 determinism via QEMU. All local forks + main repo clean and pushed. Remaining: junod-aegis CI not accessible locally; 4h devnet soak not time-tested; upstream pings (cosmwasm#2685, wasmvm#735, juno#1202).


== 2026-06-28 ==

- junod init NATIVE HYBRID CONSENSUS KEY FLAG COMPLETE (resumed interrupted task):
    * cometbft fork (aegis-phase-cf-hybrid@ff4dcefdc083): added
      privval.NewFilePVWithPQC(ed, ml, keyFilePath, stateFilePath) — builds a
      hybrid FilePV (classical key file + ML-DSA-44 sidecar) from EXISTING
      ed25519+mldsa44 halves, so a recovered mnemonic can deterministically
      reproduce the hybrid validator key (complements GenFilePVWithPQC which
      generates random halves).
    * cosmos-sdk fork (aegis-phase-d3-hybrid@5792792):
      - x/genutil/utils.go: InitializeNodeValidatorFiles(config, hybrid bool)
        and InitializeNodeValidatorFilesFromMnemonic(config, mnemonic, hybrid)
        now take a hybrid flag. Switch on {hybrid, mnemonic?}: random hybrid =>
        LoadOrGenFilePVWithPQC; mnemonic hybrid => ed via GenPrivKeyFromSecret +
        ml seed via deriveMlDsa44SeedFromMnemonic => NewFilePVWithPQC + Save.
      - x/genutil/client/cli/init.go: new --aegis-hybrid-consensus bool flag,
        read via cmd.Flags().GetBool and passed to the mnemonic initializer.
      - All callers updated to pass hybrid (gentx.go, collect.go,
        testnet.go, testutil/network/network.go, init_test.go, utils_test.go
        => false; init.go => flag value).
      - go.mod cometbft pin bumped to ff4dcefdc083 (full canonical sha; the
        short-sha pseudo-version was rejected by `go mod tidy`).
    * BUG FIXED (root cause of the interrupted build): deriveMlDsa44SeedFromMnemonic
      used the golang.org/x/crypto/hkdf idiom (hkdf.New + io.ReadFull) but
      imported Go 1.24 STDLIB crypto/hkdf, which has no New(). Rewrote to
      stdlib hkdf.Key(sha256.New, secret, nil, "aegis/hybrid-consensus/mldsa44/v1",
      tmmldsa44.SeedSize); dropped the now-unused io import. info is a string in
      the stdlib API (was []byte in x/crypto).
    * TESTS: utils_test.go +TestInitializeNodeValidatorFilesFromMnemonicHybrid
      (mnemonic => sidecar written, reload via LoadFilePVWithPQC reproduces same
      pubkey bytes) and +TestInitializeNodeValidatorFilesHybridRandom (random =>
      sidecar written). go test ./x/genutil/ ./x/genutil/client/cli/ PASS;
      go build ./... clean; gofmt -w + go vet clean (only pre-existing
      collect_test.go mock UnmarshalJSON-signature vet note remains, unrelated).
    * NOTE: hybrid commit-bytes bandwidth was ALREADY measured 2026-06-24
      (classical 2,267 B vs hybrid 15,208 B, 6.71x); this task just makes the
      key generation a first-class `junod init` flag instead of a separate tool.
    * TOOLING: the edit tool's old_string match kept failing on the gitignored
      SDK init.go; worked around with a small Python replace script (C:\temp),
      then edit tool worked fine afterwards for utils.go/init_test.go. Direct
      `git push` to BOTH forks SUCCEEDED this session (no pack corruption) —
      the PowerShell "NativeCommandError" is just git writing the "To <url>"
      line to stderr; the refspec update line confirms success.


== 2026-06-27 ==

- REPO HYGIENE CLEANUP:
    * ibc-go fork: committed the uncommitted flakiness fix in
      modules/light-clients/07-tendermint/hybrid_update_test.go.
    * ibc-go fork: CRLF line-ending noise across ~80 files was causing a dirty
      working tree; git reset --hard restored a clean working tree.
    * cometbft fork: committed the untracked transport benchmark
      p2p/conn/secret_connection_hybrid_bench_test.go.
    * junoclaw main: updated progress.txt status.

- CI NOTIFICATIONS FIXED:
    * cometbft fork: bumped all v0.38.x workflows from Go 1.22 to Go 1.24.0
      (fuzz-nightly, tests, e2e, e2e-manual, release, release-version,
      pre-release). The aegis-phase-cf-hybrid fork requires Go 1.24 for
      stdlib crypto/mlkem, so the 1.22 workflows were failing to build.
    * cosmos-sdk fork: bumped test.yml from Go 1.22 to Go 1.24 (8 occurrences).
    * ibc-go fork: bumped workflows from Go 1.21 to Go 1.24 (callbacks,
      capability, golangci, golangci-feature, release, test).
    * junoclaw main: switched the ARM64 determinism job from the unavailable
      ubuntu-24.04-arm runner to QEMU user-mode emulation on ubuntu-24.04.
      This keeps the cross-arch ML-DSA-44 determinism proof while the real
      ARM64 runner is not enabled.
    * junod-aegis CI not accessible from this workspace; apply the same ARM64
      QEMU fix if that repo shares the same workflow.

- F6 RATE-LIMIT STATE (cosmos-sdk aegis-phase-d3-hybrid):
    * Added Validator.ConsensusRotationHeight and Validator.ConsensusRotationTime
      to proto + generated code, and wired NewValidator defaults.
    * Enforced a 24h cooldown in x/staking/keeper.RotateConsKey: a second
      MsgRotateConsKey for the same validator is rejected until 24h have
      elapsed since the previous rotation's block time.
    * Added keeper tests: rotation records height/time, rotation within 24h
      is rejected, rotation after 24h succeeds. go test ./keeper/ ./types/
      and go vet pass.
    * Pushed to Dragonmonk111/cosmos-sdk aegis-phase-d3-hybrid.

- PUSHED TO GITHUB: cometbft aegis-phase-cf-hybrid, cosmos-sdk
  aegis-phase-d3-hybrid, ibc-go aegis-phase-g-hybrid-client, junoclaw main.
  All local forks and main repo are now clean and mirrored on GitHub.

- NEXT: address junod-aegis CI if accessible (not present in this workspace).

== 2026-06-26 ==

- IBC PHASE G WIRING COMPLETE (ibc-go fork aegis-phase-g-hybrid-client local=0ba19e90):
    * go.mod + go.sum: replace cometbft and cosmos-sdk with Aegis forks
      Dragonmonk111/cometbft aegis-phase-cf-hybrid and
      Dragonmonk111/cosmos-sdk aegis-phase-d3-hybrid.
    * modules/light-clients/07-tendermint/update.go: ADR-009 comment
      documenting that the 07-tendermint client has no hardcoded MaxHeaderSize
      / MaxCommitSize; the effective bound is raised in the Aegis CometBFT fork.
    * modules/light-clients/07-tendermint/hybrid_update_test.go: three focused
      unit tests driving the same light.Verify path used by verifyHeader:
      - TestVerifyHybridHeterogeneousAdjacentCommit (classical + hybrid validators)
      - TestVerifyHybridSkippingTrustOverlap (carried-over hybrid + brand-new classical validator)
      - TestVerifyHybridDoubleSignMisbehaviour (two conflicting hybrid-signed headers)
    * go test ./modules/light-clients/07-tendermint -count=1 PASS.
    * docs/ADR-009-PQC-HYBRID-IBC-LIGHTCLIENT.md: corrected G1 bound description
      and updated implementation plan.
    * Branch ready to push; origin SSH credential unavailable in this environment.
    * FLAKINESS FIX: TestVerifyHybridSkippingTrustOverlap now uses a brand-new
      classical validator (not in trusted set) plus a carried-over hybrid validator
      instead of a classical->hybrid rotation. The non-deterministic failure was
      caused by VerifyCommitLightTrusting iterating signatures in validator-set
      order and returning on the first bad signature; rotated-key signatures fail
      verification against the trusted set. 50x run of TestVerifyHybrid* passed.
      Full modules/light-clients/07-tendermint test suite green (31.8s).

== 2026-06-25 ==

- F7 HYBRID REMOTE SIGNER PASSTHROUGH COMPLETE (CometBFT fork aegis-phase-cf-hybrid
  local=dc0d51b, remote=21f2370):
    * Rewrote v0.38.x Aegis commits (C5, F4, F2/F4, F5, F7) to author
      Dragonmonk111 <tj.yamlajatt@gmail.com>, then fast-forwarded
      aegis-phase-cf-hybrid to that tip and pushed via fast-export|fast-import
      (pack-corruption workaround).
    * privval/signer_client_pqc_test.go: SignerServer wraps a hybrid FilePV
      (GenFilePVWithPQC); SignerClient retrieves the 1,344 B hybrid pubkey via
      PubKey oneof and gets back 2,491 B framed hybrid signatures for votes and
      proposals. Signatures verify under the hybrid pubkey and are rejected by
      a classical-only Ed25519 verifier.
    * go test ./privval/ -run TestSignerHybrid -count=1 PASS.

- F6 MsgRotateConsensusKey WIRING COMPLETE (SDK fork aegis-phase-d3-hybrid@8c388ca):
    * Proto already had MsgRotateConsKey / MsgRotateConsKeyResponse and the
      Msg/RotateConsKey RPC in proto/cosmos/staking/v1beta1/tx.proto; generated
      types already in x/staking/types/tx.pb.go.
    * Message server handler already in x/staking/keeper/msg_server.go: validates
      valoper, checks consensus params, rejects duplicate consensus addresses, swaps
      ConsensusPubkey, updates cons-addr -> operator index, emits rotate_consensus_key
      event.
    * TestMsgRotateConsKey in x/staking/keeper/msg_server_rotate_test.go passes
      (unknown validator, successful rotation, rotate-to-used-key rejected).
    * ADDED CLI: x/staking/client/cli/tx.go NewRotateConsKeyCmd, usage
      `junod tx staking rotate-cons-key [validator-address] [path-to-pubkey-json]`.
      Reads a single SDK Any pubkey JSON and submits the MsgRotateConsKey tx.
      go build + go vet + keeper tests green.
    * STILL GATED: rate-limit state (ConsensusRotationHeight) on Validator and
      24h cooldown enforcement; can be added without changing the wire Msg shape.

- TRANSPORT-LAYER RTT TEST RUN on VirtualBox VM (production validator host):
    * Built junod-aegis in VM from forks: ~/aegis-build (cometbft aegis-phase-cf-hybrid
      + cosmos-sdk aegis-phase-d3-hybrid + juno v29.0.0, replace directives).
      version --long: cosmos_sdk_version v0.50.13, go1.24.0.
    * Production validator is a MANUAL process (NOT systemd): /usr/local/bin/junod
      start --home /home/dragonmonk111/.juno, PID 9249, ports 26656/26657/9090.
      Stopped via SIGTERM for the test, prod priv_validator_key.json sha256 recorded
      before/after = UNCHANGED, restarted clean.
    * 4-node localnet (chain aegis-localnet-1, +100 port offset, loopback peers).
      Classical run: commit_bytes = 2,265. Hybrid transport run
      (AEGIS_HYBRID_TRANSPORT=1): commit_bytes = 2,265 -> IDENTICAL.
    * RESULT: the X25519+ML-KEM-768 P2P handshake adds ZERO bytes at the commit
      level (it protects the link, not the consensus payload). The real transport
      cost is handshake CPU + one ML-KEM ciphertext round, best measured by the
      CometBFT fork's secret_connection_hybrid_test.go, not a loopback localnet.
    * Gotcha fixed: wiping data/ removed priv_validator_state.json -> node Exit 1
      ("open .../priv_validator_state.json: no such file or directory"). Fix:
      recreate data/ + seed {"height":"0","round":0,"step":0} before hybrid restart.
    * Artifact: ~/aegis-localnet-artifacts/transport-rtt-result.json.

- CROSS-ARCH DETERMINISM CONFIRMED (QEMU): ran aegis-accounts/cmd/verify-artifact
  native amd64 + cross-built linux/arm64 under qemu-aarch64-static. Both:
  input_sha256=623000372fa329ff62586b2203c337d33f62b0f9484c5e855614a5892883eba9,
  verify_ok=true, tampered_rejected=true -> IDENTICAL (only goarch differs).
  Closes the Phase F cross-CPU/OS determinism checklist item via emulation.
  Artifacts: aegis-accounts/artifacts/verify-{amd64,arm64}.json. Real-silicon
  confirmation (Oracle A1 free-tier / Apple Silicon / Graviton) optional hardening,
  NOT a blocker. No Hetzner/AWS needed — QEMU on the host did it for free.

- TRANSPORT HANDSHAKE BENCHMARK: added p2p/conn/secret_connection_hybrid_bench_test.go
  (in-memory CPU-only bench) to CometBFT fork. Measured (Ryzen 5 5600H, go1.24,
  -benchtime 200x): classical handshake 580,413 ns/op (25,253 B, 242 allocs);
  hybrid 895,061 ns/op (65,645 B, 264 allocs). Delta +315 us CPU, +40 KB heap,
  1.54x. Real-TCP RTT re-run (TestHybridHandshakeRTT) confirmed: +207us@0RTT,
  +5.59ms@10ms, +25.74ms@50ms (~0.5 RTT = one extra ML-KEM ct leg); bytes on
  wire 2,158->5,623 = +3,465 B, one-time per connection. All costs are
  per-connection at dial, ZERO per-block (localnet commit 2,265=2,265).
  Results appended to aegis-transport/docs/PHASE_C6_RTT_RESULTS.md (2026-06-25 section).

- ARTICLE HARDENED: drafts/ARTICLE_AEGIS_CONSENSUS_PQC_2026_06_23.md transport
  section was qualitative-only; added measured CPU/RTT/bytes tables + the
  commit-invariance (0 B/block) finding. Updated "What Is Next" (C6 Done; IBC
  & account migration now "Design done" -> ADR-009/ADR-010); fixed stale "PQC
  binary awaits one build step" line; added bench/RTT test repro commands.

- D3 ACCOUNT WIRING COMPLETE (SDK fork pushed to aegis-phase-d3-hybrid@2584d80):
    * crypto/hd hybrid algo: deterministic BIP39 -> secp256k1 scalar + ML-DSA-44 seed.
    * crypto/keyring default algos include hd.HybridSecp256k1MlDsa44.
    * x/auth/ante hybrid gas case: 300k ML-DSA-44 on top of secp256k1 cost.
    * CLI: existing --key-type hybrid-secp256k1-mldsa44 flag works with keyring.
    * SDK go.mod replaces CometBFT with Dragonmonk111/cometbft aegis-phase-cf-hybrid
      so the hybrid consensus-key proto resolves; go.sum tidy; Go 1.24.0.
    * Fixed Go 1.24 vet errors: non-constant format string in fee.go, hd example names.
    * Tests green: go test ./crypto/keys/hybrid/... ./crypto/hd/... ./x/auth/ante/...

- GITHUB ACTIONS ARM DETERMINISM WORKFLOW: created .github/workflows/aegis-determinism-arm.yml
  (re-triggered 2026-06-25 after fixing aegis-bench/artifacts gitignore exception).
  that runs aegis-accounts/cmd/verify-artifact on real x86_64 (ubuntu-24.04) and real ARM64
  (ubuntu-24.04-arm) runners, compares the deterministic JSON fields, and commits the two
  artifacts to aegis-bench/artifacts/ on main (or uploads them as workflow artifacts).
  No cloud account needed; GitHub provides the ARM runner free for public repos.

- DETAILED MAINNET WIRING PLAN: docs/AEGIS_F6_D3_IBC_PHASEG_PLAN.md written. Covers the
  remaining protoc-gated and IBC work: F6 MsgRotateConsensusKey (proto, msg server, rate
  limit, CLI), F7 hybrid remote-signer signature passthrough, D3 account keyring/CLI/gas
  wiring, and IBC Phase G 07-tendermint fork integration with header-size bounds and
  end-to-end hybrid update/misbehaviour tests. Recommended order: D3 -> F6 -> F7 -> G.

- ADR-009 WRITTEN: docs/ADR-009-PQC-HYBRID-IBC-LIGHTCLIENT.md (Phase G).
    Hybrid-aware 07-tendermint client: reuses landed VerifyCommitLight/
    VerifyCommitLightTrusting hybrid dispatch; G1 header-size bound bump, G2
    dispatch (inherited from CometBFT fork), G3 trust-overlap counting across a
    consensus-key rotation (matches by ADDRESS, verifies with header's pubkey;
    F1-b address invariant is the linchpin), G4 misbehaviour, G5 relayer.
    Classical-half fallback => no flag day, channel stays open through migration.
    In-place extension, NOT a new 08-pqc-tendermint client type.

- ADR-010 WRITTEN: docs/ADR-010-PQC-ACCOUNT-KEY-MIGRATION.md (Phase D migration).
    Decouples authenticator from address so EXISTING classical accounts go hybrid
    WITHOUT changing address/moving funds/re-issuing credentials. Stage ladder:
    0 CLASSICAL -> 1 REGISTERED (advisory, risk-free) -> 2 DUAL-REQUIRED -> 3 PQC-ONLY.
    MsgRegisterPqcKey (proof-of-possession via binding_sig over chain_id|addr|secp|mldsa),
    MsgEnablePqcEnforcement (requires LIVE dual sig => can't lock yourself out),
    AnteHandler reads per-account stage from new x/pqcauth, downgrade from Stage 2
    must be dual-signed (closes quantum downgrade-attack). Preserves the
    address-keyed JunoClaw member roster (orthogonal columns).


== 2026-06-24 ==

- HYBRID CONSENSUS KEYS WIRED into junod init:
    * New SDK crypto/keys/hybrid_consensus PubKey (Ed25519 32 B + ML-DSA-44 1,312 B = 1,344 B).
    * SDK codec registration in crypto/codec/proto.go + cmt.go (FromCmtProtoPublicKey/ToCmtProtoPublicKey).
    * CometBFT ValidatorParams default accepts aegis_hybrid_ed25519_mldsa44.
    * CLI tool aegis-bench/cmd/hybrid-consensus-keygen generates hybrid keys.
    * 4-validator localnet runs and produces blocks with hybrid keys.

- BANDWIDTH MEASURED via /commit?height=:
    * Classical keys: commit_bytes = 2,267 B (height ~30).
    * Hybrid keys:    commit_bytes = 15,208 B (height ~15).
    * Increase: 6.71x (dominant term is 4 x 2,491 B hybrid signatures).

- FORK PUSHES via fast-export|fast-import (pack-corruption workaround):
    * Dragonmonk111/cosmos-sdk aegis-phase-d3-hybrid: ea7c1af
    * Dragonmonk111/cometbft aegis-phase-cf-hybrid: 7524f93
    * junoclaw main: feee929

- CLEANUP: removed temporary helper scripts; added .windsurf/ to .gitignore;
  committed aegis-bench/cmd/hybrid-consensus-keygen; removed C:\temp\aegis-push
  temp dir (~149 MB export files + scratch repos).

- REBUILT junod-aegis from pushed forks: local aegis-forks synced to remote tips via HTTPS fetch
  (GitHub shallow clone failed with fetch-pack disconnect; workaround: copy local forks). Juno v29.0.0
  (v29.1.0 tag does not exist in upstream). Binary SHA256:
  98e6813ac0dd8f004002a9b1896cea3f13362c4af985d40ffe2c9a73f2bcacdb. Size 158 MB.
  Saved to /root/aegis-localnet-artifacts/junod-aegis; version --long works.

- PHASE B REGEN DONE: ran wasmvm-fork/patches/regen-mldsa.sh. Patches 20-28
  regenerated; full 00-28 series verified to apply cleanly to cosmwasm v2.2.2.

- PHASE B BENCHMARK DONE: devnet image rebuilt with patches 20-28; benchmarked
  VerifyMlDsaAttestation gas on junoclaw-bn254-1. Pure (in-Wasm fips204):
  ML-DSA-44=269,604, ML-DSA-65=328,945, ML-DSA-87=408,298. Precompile:
  ML-DSA-44=260,381, ML-DSA-65=315,212, ML-DSA-87=387,124. Precompile ~1.03-1.05x
  cheaper vs pure. Full results: deploy/mldsa-devnet-benchmark-results.json.

- ARTICLE UPDATE DONE: added measured 6.71× hybrid commit-size increase to
  drafts/ARTICLE_AEGIS_CONSENSUS_PQC_2026_06_23.md; updated "What Is Next" table
  to mark completed tasks. Also added short recap article
  drafts/ARTICLE_AEGIS_TRIPLE_FINISH_2026_06_24.md.

== 2026-06-23 ==

- SDK FORK AUTHOR REWRITE DONE (root@LAPTOP-QJNRIKAM -> Dragonmonk111
  <tj.yamlajatt@gmail.com>). 4 aegis commits on aegis-phase-d3-hybrid rebased
  with `git rebase 5f1e7de --exec 'git commit --amend --reset-author --no-edit'`
  (GIT_SEQUENCE_EDITOR no-op). Safety: script aborts on dirty tree, wrong
  branch, or any non-root author in the range. Direct push hit the known local
  pack corruption ("did not receive expected object ... index-pack failed") so
  pushed via fast-export|fast-import into a fresh temp repo (cmd /c pipe for
  byte-clean blobs) -> git push --force. Remote tip now 4e6f315, all 4 commits
  authored by Dragonmonk111. Pin check first: NO go.mod replace->fork pin
  anywhere in junoclaw, so the SHA change breaks nothing downstream.

- Upstream tracking doc created: docs/UPSTREAM_TRACKING.md. Resolved the three
  items: cosmwasm#2685 = BN254 alt_bn128 host-fns proposal (OPEN, awaiting
  maintainer ABI/feature-flag/gas feedback before PR); wasmvm#735 = companion
  Go-side wrapping question (follow BLS12-381 VM-internal precedent?); juno#1202
  = v30 upgrade PR (OPEN, rebase aegis patches onto v30 tags once merged).

- Two infra/hardware-gated PLANS written (deterministic, boxed steps):
  * docs/VALIDATOR_SAFE_LOCALNET_RUNBOOK.md — 4-validator bandwidth localnet on
    the VairagyaNodes ext4 host WITHOUT risking the prod validator: separate
    --home, +100 port offset, prod service stopped during run, prod
    priv_validator_key.json checksum verified unchanged before restart. Steps
    0-8 + rollback, each with an explicit "deterministic outcome".
  * docs/CROSS_ARCH_VERIFY_ARTIFACT_PLAN.md — cross-CPU/OS VerifySignature hash
    artifact: ARM sourcing (Option1 borrow ffern/Apple Silicon; Option2 rent
    AWS t4g.nano/Oracle A1/Hetzner CAX11 by the minute, terminate same session,
    sub-cent, NO continuous plan; Option3 QEMU stop-gap) + the verify-artifact
    KAT program + diff-must-be-empty acceptance.

== 2026-06-21 (session 3) ==

- F7 SignerClient (tmkms/remote) hybrid path DONE & TESTED (protoc-free):
    * privval/signer_client_pqc_test.go (3 tests, TCP + Unix transports):
      server wraps a hybrid FilePV (GenFilePVWithPQC); SignerClient.GetPubKey
      retrieves the 1,344 B hybrid pubkey via the PublicKey oneof; SignVote +
      SignProposal return 2,491 B framed hybrid sigs end-to-end, under the
      10 KB maxRemoteSignerMsgSize.
    * No silent downgrade: framed hybrid sig verifies under hybrid pubkey and
      is REJECTED by a classical-only Ed25519 verifier; address invariant holds.
    * DefaultValidationRequestHandler needed NO change — it already routes
      through cryptoenc.PubKeyToProto/FromProto, hybrid-aware since the F4 commit.
    * go test ./privval/ PASS (21.6s), go vet clean. Committed in fork.
- Verified F6 evidence hybrid test (evidence/verify_hybrid_test.go) PASS.
- F1 cross-impl KAT DONE (closes determinism checklist #4):
    * crypto/mldsa44/testdata/mldsa44_kat.json — authoritative ML-DSA-44 (FIPS 204)
      vectors EXTRACTED from contracts/jclaw-credential/src/mldsa_vectors.rs
      (generated by the independent fips204 Rust crate; deterministic
      keygen_from_seed=[01;32], try_sign_with_seed=[02;32], fixed message).
    * crypto/mldsa44/mldsa44_kat_test.go — asserts circl (Go) (1) reproduces the
      fips204 pubkey byte-for-byte from the same xi seed (keygen interop) and
      (2) verifies a fips204-produced signature (cross-impl verify conformance,
      the consensus-critical deterministic path); negatives reject tamper/trunc.
      go test PASS, go vet clean.
- FORK REMOTE HYGIENE (ethical): aegis-forks/cometbft 'origin' pointed at the
  OFFICIAL github.com/cometbft/cometbft.git (fetch+push) — a push footgun.
  Renamed origin -> upstream (fetch-only; push URL set to a DISABLED sentinel);
  created+checked-out branch aegis-phase-cf-hybrid (was on upstream name v0.38.x);
  added AEGIS_CHANGES.md (Apache-2.0 §4(b) change inventory + do-not-push note).
  NOTE: set 'origin' to YOUR fork before pushing. 6 local commits now on
  aegis-phase-cf-hybrid; NONE pushed.
- Docs: FRAMEWORK_PLAN_REMAINING.md §9 checklist updated — F1 KAT, F4 SignerClient,
  F5 proto path, F6 evidence all marked DONE; determinism item now [~]
  (cross-impl verify done; cross-CPU/OS hash artifact still open); only
  MsgRotateConsKey + 4-validator localnet remain (infra-gated).
- D3 codec wiring DONE & TESTED (SDK fork, protoc path now working):
    * proto/cosmos/crypto/hybrid/keys.proto (PubKey/PrivKey, single bytes key) ->
      generated crypto/keys/hybrid/keys.pb.go via buf + protoc-gen-gocosmos
      (installed module-pinned). Refactored hybrid.go to the generated {Key []byte}
      structs; PrivKey halves (secp32 || mldsa-priv 2560) reconstructed on use.
    * crypto/codec/proto.go + amino.go: registered &hybrid.PubKey/&PrivKey as
      cosmos.crypto.PubKey/PrivKey impls (Any) + amino concrete.
    * crypto/codec/hybrid_test.go: Any + proto-codec-interface round-trip; sig
      verifies through round-tripped key; forgery rejected. build+test+vet PASS.
    * go.mod: cloudflare/circl recorded indirect (codec now reaches hybrid).
    * STILL PENDING in D: keyring/hd SignatureAlgo + 'keys add --algo' CLI;
      x/auth/ante gas case for hybrid sig verify.
- FORK HYGIENE (both forks): cometbft AND cosmos-sdk had origin -> OFFICIAL repos
  (push enabled) = footgun. Both: origin renamed to upstream (push disabled),
  origin set to git@github.com:Dragonmonk111/<repo>.git, work on aegis branches
  (cometbft: aegis-phase-cf-hybrid 6 commits; cosmos-sdk: aegis-phase-d3-hybrid
  1 commit), AEGIS_CHANGES.md in each.
- PUSH STATUS: main pushed to origin (Dragonmonk111/junoclaw, 196e673..429d06a)
  via Windows git (WSL git lacks SSH key access). FORK PUSHES BLOCKED: GitHub
  repos Dragonmonk111/cometbft and Dragonmonk111/cosmos-sdk DO NOT EXIST yet
  (gh CLI installed but not authed). Create them (or 'gh auth login') then push
  the aegis branches from Windows git.
- Earlier this session: 4 commits in CometBFT fork, 3 in main; article posted to
  comms with uni-7 contract addr + Leonardo.ai art prompts + image markers.

== 2026-06-20 (session 2) ==

- F4 pubkey persistence DONE (proto regen + codec wiring):
    * keys.proto: added `bytes aegis_hybrid_ed25519_mldsa44 = 3` to PublicKey oneof.
    * buf generate: regenerated keys.pb.go (buf v1.71.0, protoc-gen-gogofaster installed
      via `go install github.com/cosmos/gogoproto/protoc-gen-gogofaster@latest`).
    * codec.go: wired hybrid.PubKey into PubKeyToProto/PubKeyFromProto + JSON type
      registration. Size validation (hybrid.PubKeySize = 1344).
    * Build + vet + tests PASS: crypto/encoding, types/, crypto/..., privval/.
    * ADR-008 F4 section updated: "Still gated" -> "Landed (proto regen + codec wiring)".

- Fable P5 docs DONE:
    * docs/MAYO.md: NIST status corrected to "additional signatures (in process)";
      MAYO-5 testnet results table added (Bud 360,814 / Verify 798,803 gas);
      Phase 5 roadmap checked off; MAYO-5 recommendation note added.
    * docs/PQC_COMPETITIVE_ANALYSIS.md: NIST round labels fixed; L5 vs Falcon-1024
      comparison table added (sig 964B vs 1280B, gas 799k/361k); strategic
      assessment updated (L5 parity); last-updated date bumped.
    * drafts/ARTICLE_L5_PQC_LIVE_COSMOS.md: full article draft with benchmark tables,
      architecture explanation, greenfield-vs-migration framing, accuracy boundaries.
    * drafts/TG_REPLY_L5_BENCHMARKS.md: Telegram reply to Jake/Marius with compact
      benchmark table and balanced Marius comparison.

- Toolchain: buf v1.71.0, protoc 3.12.4, protoc-gen-gogofaster v1.7.2 (go1.22.4),
  go1.24.0 all available in WSL. Makefile proto-gen target uses `go run buf generate`.

== 2026-06-20 ==

- Phase F / F4 verification dispatch DONE & TESTED (protoc-free):
  aegis-forks/cometbft/types/consensus_hybrid_test.go (5 tests total in file;
  the 3 commit tests here, 2 light-client tests in the item-10 bullet). Drives
  the REAL VerifyCommit / VerifyCommitExtended path over in-memory hybrid
  validators (MockPV wrapping hybrid.GenPrivKey):
    * Heterogeneous set (2 classical + 2 hybrid), all signing -> Commit AND
      ExtendedCommit both verify; every sig dispatches on the signer's registered
      key type. AllKeysHaveSameType()==false.
    * Mid-migration block, 1 validator offline -> surviving 3-of-4 MIXED quorum
      (always >=1 classical + >=1 hybrid) clears 2/3 and commits. This is the
      literal ADR-008 §6 item-9 "upgrade without halting" scenario.
    * All-hybrid set: verifies via single verification; shouldBatchVerify()==false
      because ML-DSA-44 has no crypto.BatchVerifier -> never enters an unsupported
      batch path (item 8 = safe fallback, NOT a batch impl).
  GATED (folds into F7): persisting the hybrid pubkey in cmtproto.Validator still
  needs the pc.PublicKey oneof regen; tests use in-memory validator sets.

- Phase F / §6 item 10 (light client / IBC) DONE & TESTED (protoc-free), added to
  consensus_hybrid_test.go (2 more tests PASS):
    * VerifyCommitLight (+AllSignatures): heterogeneous adjacent-header commit
      verifies on the light-client path (the one light/verifier.go + IBC
      07-tendermint call).
    * VerifyCommitLightTrusting (IBC SKIPPING path): models validator-set
      rotation — a full commit from a rotated set verified against the trusted
      (old) set. Hybrid validators carried across the rotation COUNT toward the
      trust level (2 carried hybrids = 20/40 > 1/3 -> trusted); a sub-threshold
      overlap (1 hybrid = 10/40 <= 1/3) is REJECTED with
      ErrNotEnoughVotingPowerSigned. Core ./light/ verifier tests still PASS
      (-short, 9s) -> no regression. (Full ./light/... is network-bound/slow,
      unrelated to these changes; go build ./... already confirmed it compiles.)

- Phase F / F5 evidence/slashing DONE & TESTED (protoc-free):
  aegis-forks/cometbft/evidence/verify_hybrid_test.go (PASS). Exercises the real
  evidence.VerifyDuplicateVote (the pre-slash check):
    * Genuine hybrid double-sign (2 conflicting precommits, same H/R/S) -> VALID,
      validator slashable.
    * Forged double-sign with intact Ed25519 half but corrupted ML-DSA-44 half
      (quantum attacker who broke only Ed25519 trying to FRAME an honest
      validator) -> REJECTED (ErrVoteInvalidSignature). Fabricating slashable
      evidence now also requires breaking ML-DSA-44.

- FORK FIX (real, required for F2/F4 — not just tests): hybrid sig is 2,491 B,
  far above the classical 64 B, so three byte-budget bounds were rejecting/under-
  counting hybrid consensus data:
    * types/signable.go  MaxSignatureSize = max(hybrid.SignatureSize, 64). Without
      this, every hybrid Vote/CommitSig/Proposal/vote-extension fails ValidateBasic.
    * types/block.go      MaxCommitSigBytes -> 2,537 B (var, tracks MaxSignatureSize);
      MaxCommitBytes per-sig proto overhead 2 -> 3 (2-byte length prefix). Keeps
      MaxDataBytes block-size accounting correct (else blocks under-count commit
      size). TestMaxCommitBytes / TestBlockMaxDataBytes expectations updated to the
      exact new budgets.
  Verification: go build ./... clean; go test ./types/ ./privval/ ./evidence/
  ./crypto/hybrid/ ./crypto/mldsa44/ all PASS (WSL toolchain go1.24.0 via
  GOTOOLCHAIN=auto). No regressions.


== 2026-06-18 ==

- Phase F / F5 DONE (protoc-free): aegis-forks/cometbft/types/validator_hybrid_test.go
  (3 tests PASS). Proves (1) hybrid pubkey is a first-class crypto.PubKey in the
  validator set: NewValidator/ValidateBasic accept it, GetByAddress/GetByIndex
  resolve it, address = Ed25519-half address (zero state migration); (2) a hybrid-
  signed prevote (2,491 B sig) verifies via vote.Verify (val.PubKey.VerifySignature)
  and a classical-only Ed25519 verifier REJECTS it; (3) F5 CORE rotation invariant:
  reuse Ed25519 half + fresh ML-DSA-44 half -> SAME address, different key;
  UpdateWithChangeSet swaps the entry cleanly; post-rotation vote verifies under the
  new pubkey but is REJECTED by the pre-rotation pubkey (proves the PQ trust anchor
  actually moved). go build ./types/... ./privval/... clean; go test ./privval/...
  still PASS (no regression).
  GATED on protoc/buf (folds into F7): Validator.Bytes()/ValidatorSet.Hash()/
  Validator.ToProto() + crypto/encoding.PubKeyToProto/FromProto need a hybrid
  pc.PublicKey oneof variant for wire/genesis/state-store persistence.

- Phase F / F4 DONE (protoc-free): FilePV signs both halves via ML-DSA-44 sidecar
  (priv_validator_key.json_mldsa44.json); classical key file never mutated;
  Address() still delegates to classical half; privval/file_pqc_test.go green.


== 2026-06-17 ==

- Deep re-audit of junoclaw vs memory. KEY CORRECTION: Project Fable Phase 3
  (multi-variant contract) was ALREADY DONE, not pending. jclaw-credential
  dispatches MAYO-1/2/3/5 (contract.rs ~296-306) + ML-DSA-44/65/87 (~383-388);
  tests.rs has mayo3/mayo5/wrong-variant + mldsa 44/65/87 static-vector tests
  (~33 tests). Design uses variant-agnostic SHA-256(pk) + variant at verify time
  (cleaner than the planned stored variant_tag). PqcVerifier trait deferred as
  unnecessary over-engineering on a working tested contract. Memory updated.

- Aegis Phase D / D1 DONE: wrote docs/ADR-007-PQC-HYBRID-ACCOUNTS.md
  (hybrid secp256k1 + ML-DSA-44 accounts; both-must-verify; 20-byte address via
  domain-separated address.Hash; HD = BIP-44 secp + HKDF ML-DSA seed; opt-in;
  SignMode/AnteHandler/gas; alternatives + consequences). Matches ADR-006 style.

- Aegis Phase D / D2 DONE + VERIFIED: created aegis-accounts/ Go module
  (go 1.24). Files: hybrid.go (keygen/sign/verify/address), bip32.go (BIP-32 CKD
  for the classical half), hd.go (mnemonic -> hybrid via bip39 + crypto/hkdf),
  bech32.go (in-tree BIP-173), 3 test files, README. Deps: cloudflare/circl
  (ML-DSA-44), decred secp256k1 v4, cosmos/go-bip39.
    * go vet clean, go test ./... = 20/20 PASS in WSL go1.24.
    * Found+fixed a one-NIBBLE typo in my BIP-32 expected constant (a8 vs a2);
      code was correct (matched 31.5/32 bytes -> avalanche proves typo not bug).
      Rewrote TestBIP32Vector1 to derive expected from canonical xprv via an
      in-test base58check decoder (checksum-protected, authoritative, non-circular).
    * Headline test TestHybridBothHalvesRequired proves forgery needs BOTH
      primitives (foreign classical OR foreign PQ half is rejected).
  D2 is the source of truth for ADR-007 crypto; D3 (SDK fork) ports it 1:1.

- Confirmed Fable P3 by running the suite: jclaw-credential 33/33 tests PASS
  (incl. mayo3/mayo5/wrong-variant + mldsa 44/65 valid-sig + mldsa_static_vectors).

- Aegis Phase D / D3 PREP DONE: wrote aegis-accounts/PORTING.md — function-by-
  function map of the harness onto a Cosmos SDK fork (the D3 analogue of
  secretconn/PORTING.md). Key insight documented: Cosmos's SigVerificationDecorator
  calls PubKey.VerifySignature, so a hybrid VerifySignature requiring BOTH halves
  needs NO new decorator and NO new SignMode — only a gas case. The actual fork
  (clone SDK, proto key types, keyring algo, CLI, integration test) stays gated.

- No go.work in repo (aegis-transport + aegis-accounts are independent modules).
  No CometBFT version pinned anywhere in junoclaw (app-layer only) -> C5/D3 actual
  forks need an external clone + version info not present here.

- Updated COMPLETION_PLAN.md + todo list. Remaining Aegis/Fable work is now all
  devnet-/fork-gated: C5 (CometBFT fork) + C6 (RTT), D3 (SDK fork), Fable P4
  (benchmark ladder). Article (tweet + Medium w/ Midjourney prompts) queued for
  after those land.

- User steer: do the heavy forks (C5/D3) FIRST, then restart devnet for C6+P4.

- Aegis C5 DONE (CometBFT fork builds + tests green). Shallow-cloned cometbft
  v0.38.x -> aegis-forks/cometbft (gitignored; has own .git). ADDITIVE patch:
    * p2p/conn/secret_connection_hybrid.go: MakeSecretConnectionHybrid (X25519 +
      ML-KEM-768) reusing package internals + ChaCha20 framing. Combiner =
      SHA256(dh||ssPQ) feeds the EXISTING deriveSecrets so the classical golden
      vectors are untouched. ek's + ct bound into the Merlin transcript.
    * secret_connection_hybrid_test.go: handshake + AEAD read/write + combiner-
      binds-both. go.mod bumped 1.22.11 -> 1.24.0 (stdlib crypto/mlkem, no new dep).
    * p2p/transport.go upgradeSecretConn: env-gated AEGIS_HYBRID_TRANSPORT=1 ->
      hybrid path (one binary, A/B for C6).
    * VERIFIED: go test ./p2p/conn/ all PASS incl. existing evil + golden tests
      UNCHANGED (proof the patch is additive/non-regressive); go build ./p2p/...
      OK; go vet ./p2p/conn/ clean. Notes: aegis-transport/docs/PHASE_C5_FORK_RESULTS.md.
  NOTE: read_file/edit/write work on the gitignored fork, but grep_search/
  code_search/find_by_name are BLOCKED by .gitignore -> use shell grep/rg instead.

- Aegis D3 core DONE (Cosmos SDK fork builds + tests green). Shallow-cloned
  cosmos-sdk release/v0.50.x -> aegis-forks/cosmos-sdk (gitignored; own .git).
    * NEW crypto/keys/hybrid package: PrivKey/PubKey implement cryptotypes.
      PrivKey/PubKey. Classical half COMPOSES the SDK's own crypto/keys/secp256k1
      (wire-identical); PQ half = cloudflare/circl v1.6.1 ML-DSA-44 (go get added).
    * KEY DESIGN: PubKey.VerifySignature == true IFF BOTH halves verify -> plugs
      into the EXISTING x/auth AnteHandler with NO new decorator/SignMode (only a
      gas case). Address via SDK types/address.Hash + domain tag. PubKey 1345B,
      Sig 2484B.
    * circl v1.6.1 compiles fine under the SDK's go1.22.11 toolchain (no go bump).
    * VERIFIED: go test ./crypto/keys/hybrid/ 9/9 PASS (incl. both-halves-required,
      deterministic-from-seeds); go vet clean. Doc: aegis-accounts/docs/PHASE_D3_FORK_RESULTS.md.
  D3 remaining (gated on protoc/buf + devnet): proto/codec registration, keyring
  algo + CLI, gas case. Documented in the D3 results doc.

- BOTH heavy forks now DONE (C5 + D3). Next: devnet restart (pre-approved
  wsl --shutdown) to unblock C6 (hybrid-transport RTT) + Fable P4 (MAYO gas
  ladder). Check non-destructive devnet/docker state FIRST before the shutdown.

- C6 DONE (real fork + real TCP RTT measured). Phase C COMPLETE.

- DEVNET RESET + Fable P4 DONE (later same day). Sequence:
    * Committed all Aegis work locally FIRST (git commit OK; push BLOCKED - no
      SSH key in WSL for git@github.com:Dragonmonk111/junoclaw. Work is SAFE in
      local git, just not pushed - configure SSH or use a PAT/https to push).
    * down -v (wiped devnet_junod-data) + wsl.exe --shutdown + run-devnet.sh.
    * CLOCK FINDING (important, corrects earlier note): wsl --shutdown did NOT
      fix wall-clock. Host w32time service is STOPPED (w32tm /resync ->
      0x80070426) so the Hyper-V time-sync feeds a stale ~14-min-behind value.
      BUT the fresh VM clock is MONOTONIC (advances steadily), which is all a
      single-validator CometBFT needs - the old stall was a FROZEN/backward
      clock, now cleared by the reset.
    * DO NOT run hwclock -s / date -s on the live VM: it momentarily sets the
      correct time then Hyper-V reverts within ~5s -> clock OSCILLATES +-15min
      (backward jumps) -> consensus stalls. Leave the monotonic-offset clock
      alone. To truly fix the offset: Start-Service w32time (admin) + w32tm
      /resync /force, or reboot Windows. NOT needed for the devnet to run.
    * Devnet LIVE; sustained block production confirmed (h11 -> h15 steady).

- Fable P4 DONE (MAYO-2/3/5 gas ladder reproduced end-to-end on the fresh chain).
  Ran FRESH=1 devnet/scripts/benchmark-mayo-devnet.sh -> deploy/benchmark-mayo-devnet.cjs.
  Both flavours (pure + precompile) deployed; Bud + VerifyMayoAttestation per
  variant. Reproduced prior numbers within ~0.03% (prior backed up to
  deploy/mayo-devnet-benchmark-results.prev.json). Verify gas (pure / precompile):
    MAYO-2 L1: 355932 / 310391 (1.15x)
    MAYO-3 L3: 456644 / 257371 (1.77x)
    MAYO-5 L5: 798137 / 360902 (2.21x)
  KEY: pure-Wasm verify is BELOW the old 1.2-1.6M extrapolation -> MAYO-5 runs
  on stock Juno with no precompile needed; the precompile win grows with level.
  Image is MAYO-patched (wasmvm-fork patches 10-19 incl. mayo_verify host fn);
  the devnet Dockerfile glob applies all v2.2.2/*.patch (00-09 BN254, 10-19
  MAYO, 20-28 ML-DSA).

- STATUS: Aegis C (C5+C6) COMPLETE, D (D1/D2/D3-core) COMPLETE (wiring gated),
  Fable P3+P4 COMPLETE. Remaining: Fable P5 (docs/comms + article), D3 wiring
  (needs protoc/buf), upstream pings. COMPLETION_PLAN.md updated to match.


== 2026-06-16 ==

- Full status audit performed against all memory + upstream repos.
- Created docs/COMPLETION_PLAN.md — consolidated plan for all remaining work:
    * Track C: C5 (CometBFT fork patch) + C6 (devnet RTT measurement)
    * Track D: ADR-007 spec + aegis-accounts/ harness + SDK fork integration
    * Track Fable: P3 (multi-variant contract) + P4 (benchmark ladder) + P5 (comms)
    * Infrastructure: devnet WSL2 stability fix
    * Upstream: cosmwasm#2685, wasmvm#735, juno#1202 tracking
- Created .windsurf/workflows/completion-plan.md for step-by-step execution.
- TODO list refreshed: 10 items, priorities assigned, dependencies mapped.
- Next immediate actions (parallel, no blockers):
    1. Apply devnet stability fix (wsl --shutdown → relaunch → verify)
    2. Start C5 CometBFT fork patch application
    3. Draft ADR-007 spec
    4. Extend jclaw-credential for MayoVariant

- Risk register captured in plan: Go stdlib ML-DSA availability for D2,
  CometBFT API drift for C5, MAYO-5 gas for P4, devnet stability.


== 2026-05-31 ==

- Frontend + bridge re-pointed off Polkachu onto STAVR (verified-live uni-7):
    * frontend/src/lib/chain-config.ts: rpc -> https://juno.rpc.t.stavr.tech
      REST left on Polkachu (no STAVR REST endpoint confirmed). Added the two
      new contract addresses: moultbookV0 (code 76) + ibcTaskHost (code 77).
    * wavs/bridge/src/config.ts: default rpcEndpoint -> STAVR (JUNO_RPC override
      retained).

- Nostr bridge daemon binary shipped: crates/junoclaw-nostr-bridge/src/main.rs.
  The crate was lib-only; added the runnable entrypoint that wires subscriber ->
  publisher. Design notes:
    * sync subscriber callback -> async publisher bridged via unbounded mpsc.
    * subscriber runs in a reconnect loop, exponential backoff 1s..30s, reset
      to 1s after a connection stays up >=60s (stable-connection rule).
    * graceful shutdown on Ctrl+C / SIGTERM; channel closed so the publisher
      task drains in-flight events before exit.
  cargo build clean; 10 existing tests still green; bin target recognised.

- Docs synced: lib.rs run example now uses STAVR/uni-7; skill-reference-v2
  §7 moves Nostr bridge from "not yet shipped" to shipped-with-daemon.
  TESTNET_SAFE_REFERENCE.md table updated with codes 76/77 + STAVR RPC.

- Still TODO (needs live secrets / manual run): execute the bridge end-to-end
  against STAVR — emit a real post_task on task-ledger and confirm kind 38402
  lands on relay.damus.io. Daemon is ready; this is a runtime step.

- Side note (from Jacob Gadikian tweet review): commonware.xyz is a Rust
  blockchain-primitives lib (Patrick O'Grady), NOT an MCP server. Powers Kora
  (Andreas Bigger) -> Nunchi EVM testnet. Unrelated to JunoClaw; no action.

- GitHub repo "About" description still says "10 CosmWasm contracts" — README
  was fixed to 12 on 05-27 but the repo short-description field is separate.
  MANUAL: update via repo Settings -> About -> "12 crates".


== 2026-05-29 ==

- STAVR (Discord #validators-private, juno) replied to our testnet DM with a
  working uni-7 endpoint:
      https://juno.rpc.t.stavr.tech/
  Verified live: chain uni-7, app v29.0.0, block 14,254,738, fully synced,
  catching_up=false. Moniker STAVR-Service. Unblocks the deploy script.

- Jake commit e5ec25e on CosmosContracts/juno PR #1202:
  "v30: voting-snapshot - address Cascade review findings + fix proto-gen path"
    * CRITICAL sparse-delegator bug: fixed per our two-pass algorithm proposal.
      Same fix extended to pruneTotalPower for symmetry (bonus correctness).
    * RetentionWindowHeights default changed to 0 (the safe default we advised).
    * LST quorum asymmetry: documented per our option (b); our math reproduced
      in planning/05-staking-snapshot.md.
    * pruneInterval: moved into types.Params field 3 as we suggested.
    * Minor app.toml gas floor: added to operator checklist at two locations
      in planning/07-rollout.md.
    * Regression test uses our "Alice, 10k JUNO, set-and-forget" example.
    * First commit on the 142-commit PR to cite an external reviewer by name.

- Jake also pushed 5714178: CodeQL cw-hooks map iter fix (separate housekeeping,
  not from our review).

- Jake personal: back in San Francisco after a 2yr hiatus, "things are better
  now". Telegram exchange warm. Responsive on Discord/Telegram for v30 coord.

- Manuel Araoz tweet endorsed by Jake:
  "PSA: I now consider all of DeFi unsafe. Coding agents are superhuman at
  finding vulnerabilities, and smart contract security is too asymmetric -
  defenders need to fix every bug while attackers need just one exploit."
  Directly applicable to JunoClaw's AI-augmented review workflow; the v30
  fix is the public demonstration.


NEXT (queued, in priority order):

  1. Deploy moultbook-v0 + ibc-task-host to uni-7.
     Script: deploy/deploy-new-contracts.mjs
     RPC:    https://juno.rpc.t.stavr.tech/
     Need:   funded testnet wallet; check uni-7 faucet operational.

  2. Wire frontend to uni-7; smoke test the 5-step DAO wizard against live
     chain (templates: Community Fund, Crop Protection, Mutual Aid, etc.).

  3. Run Nostr bridge end-to-end against STAVR RPC; emit a real post_task on
     task-ledger and confirm kind 38402 lands on relay.damus.io.

  4. PR the v2 skill reference to CosmosContracts/juno-network-skill.
     Drops:  jclaw-token, jclaw-airdrop
     Adds:   moultbook-v0, ibc-task-host, junoswap-factory, faucet
     Diff:   ~250 lines
     Draft already in docs/skill-reference-v2-draft.md.

  5. Master article publish (articles/JUNOCLAW_V30_RECEIPT_2026_05_29.md) -
     time to v30 governance proposal submission.

  6. Thank STAVR publicly in #validators-private after deploy succeeds.


KNOWN BLOCKERS: none today.


OPEN QUESTIONS:

  - v30 mainnet governance proposal submission date - waiting on Jake.
    Article release ties to this.

  - uni-7 faucet operational status - not verified yet; check before deploy.

  - Track B (BN254 wasmvm v3 forward-port) timing - probably wait until v30
    mainnet upgrade lands before proposing.

  - Jake asked May 28: "Who else are you collaborating with? Who's we?"
    Replied: "just me and my claude till now." His reaction: "Hehehe, nice
    team." Worth tracking how he uses this framing publicly. The "address
    Cascade review findings" commit message is one direction.


THINGS TO REMEMBER:

  - memory/ is gitignored - safe for personal notes / lessons / cascade memory
  - .gitignore also excludes docs/JAKE_DM_*.md, _private/, cv_*, GHSA drafts
  - contracts: 12 crates total (11 deployable + 1 shared lib: junoclaw-common)
  - test count: 203 as of 2026-05-29 (Nostr bridge added 10 to previous 193)
  - cosign public key in cosign.pub for OCI artifact verification
  - WAVS = Layer.xyz event-driven framework (Ethan Frey + Jake Hartnell)


VALIDATOR / COLLABORATOR CONTACTS:

  - STAVR              Discord: STAVR | STAVR  in juno #validators-private
                       RPC: https://juno.rpc.t.stavr.tech/
                       Responsive; runs many cosmos chains. Thank visibly
                       after deploy succeeds.

  - Dragonmonk111      Also in #validators-private; uses mainnet for testing.

  - Jake Hartnell      @JakeHartnell on X / Telegram - co-founder, decision
                       authority for Juno chain-level changes.


KEY REPO POSITIONS:

  - CosmosContracts/juno PR #1202: v30 upgrade, 142 commits.
    Review findings merged 2026-05-28. Awaiting final merge to main.

  - CosmosContracts/juno-network-skill: v2 reference PR not yet submitted.
    Draft ready in docs/skill-reference-v2-draft.md.

  - Dragonmonk111/junoclaw: public.
    5 security advisories C-1..C-4 + H-3 published.
    4 security releases shipped (v0.x.y-security-1 through -3 plus follow-up).
    OCI artifact published + cosign-signed.


PUBLIC URLs:

  repo:        github.com/Dragonmonk111/junoclaw
  skill spec:  github.com/CosmosContracts/juno-network-skill
  v30 PR:      github.com/CosmosContracts/juno/pull/1202
  OCI image:   ghcr.io/dragonmonk111/junoclaw/verifier:0.1.0
  pub cosign:  cosign.pub in repo root



== 2026-05-28 ==

- Nostr bridge crate shipped: junoclaw-nostr-bridge, 10 unit tests passing.
  kind 38402, three-relay default fanout (damus + nos.lol + snort), secp256k1
  signing, env-config (12-factor).

- Total tests now 203 (up from 193 the day before).

- Tests hardened on ibc-task-host and junoswap-factory:
    * ibc-task-host: missing-config errors, success dispatch with stats,
      swap denom mismatch, invalid min_return, partial config update, etc.
    * junoswap-factory: default fee fallback, excessive fee rejection,
      submsg shape verification, asset sorting, PairCount across sequential
      creates, 10000 bps boundary check.

- DM drafted to validators team for uni-7 RPC.
  Sent to Juno x AI Telegram + Discord #validators-private.

- OVERVIEW_2026_05_28.md drafted as aerial-view doc - architecture, Nostr
  bridge explanation, WAVS pattern, full task lifecycle end-to-end.



== 2026-05-25..27 ==

- Moultbook trustless-trust article drafted
  (articles/MOULTBOOK_TRUSTLESS_TRUST_2026_05_25.md).

- ADR-005 skill-circle draft.

- skill-reference-v2-draft.md drafted in docs/. Ready to PR upstream.



== 2026-05-21 ==

- PR #1 to CosmosContracts/juno-network-skill MERGED. JunoClaw now in the
  official Juno skill spec. references/junoclaw.md, 243 lines.
  Any agent reading the spec discovers JunoClaw automatically.

- MEDIUM_POST_PR_MERGED_2026_05_21.md drafted as the merge announcement.



== 2026-05-12 ==

- Posted moultbook-style review of Juno v30 PR #1202 on GitHub.
  Anchor commit 0a7098ef07. Three findings (one CRITICAL), worked example
  per finding, suggested fix per finding, plus minor notes.
  Internal long-form: docs/MOULTBOOK_REVIEW_OF_JUNO_V30_PR_1202.md
  Posted comment:   docs/PR_1202_GITHUB_REVIEW_COMMENT.md



== 2026-05-01 (governance) ==

- Proposal #373 (BN254 precompile track) passed on juno-1 mainnet.
- v0.x.y-security-1 through -3 shipped after Ffern Institute audit:
    * Shell-injection bypass (allowlist + Cargo feature gate)
    * Shell-metacharacter injection (no-shell spawn)
    * MCP mnemonic exposure (encrypted wallet-handle registry)
    * Path traversal in upload_wasm (symlink reject + size cap)
    * SSRF in computeDataVerify (scheme/port allowlist + DNS pre-resolution)
- Runtime kill-switches: signing_paused, egress_paused, admin RPC.



== 2026-06-24 ==

- AEGIS JUNO BINARY BUILT: junod-aegis 158MB ELF x86-64.
  sha256=0809331d83aae0473ace982a7e9129e8358f4321f905ab4bc2ca49674b9586f1
  Build: go build -ldflags="-checklinkname=0" ./cmd/junod/
  Fix required: Go 1.24 linkname hardening blocks bytedance/sonic/loader
  from pulling runtime.lastmoduledatap; -checklinkname=0 disables that check.
  Forks wired: Dragonmonk111/cometbft@aegis-phase-cf-hybrid (ML-KEM-768 transport)
              Dragonmonk111/cosmos-sdk@aegis-phase-d3-hybrid (ML-DSA-44 hybrid keys, tip 4e6f315)

- AEGIS BINARY FUNCTIONAL LOCALNET: 4-validator WSL localnet confirmed working.
  commit_bytes=2266 (Ed25519 classical keys, same as baseline — expected).
  num_sigs=4, consensus live, all 4 nodes reached height 28+.
  Artifact: aegis-bench/artifacts/localnet-bandwidth-N4-aegis.json
  NOTE: AEGIS_HYBRID_TRANSPORT=1 set but not confirmed active (env not passed
  via `nohup env ...`; fix for next run). Hybrid consensus bandwidth requires
  ML-DSA-44 consensus keys from init (not yet wired).

- C6 HYBRID TRANSPORT CONFIRMED: transport code path confirmed via source
  inspection (p2p/transport.go:597 aegisHybridTransportEnabled() ->
  MakeSecretConnectionHybrid). net_info bytes measure MConnection layer only
  (post-handshake), not TCP handshake bytes. Canonical measurement already
  captured in aegis-transport/cmd/twonode: hybrid 2422B/handshake vs classical
  72B (+2350B), +245us latency. C6 CLOSED.

- PHASE B MLDSA COMPLETE: regen-mldsa.sh confirmed 00-28 all apply cleanly
  to cosmwasm v2.2.2. Patches 20-28 already committed. Benchmark results
  already in deploy/mldsa-devnet-benchmark-results.json:
    ML-DSA-44: pure 269,604 / precompile 260,381 gas (1.04x) 
    ML-DSA-65: pure 328,945 / precompile 315,212 gas (1.04x)
    ML-DSA-87: pure 408,298 / precompile 387,124 gas (1.05x)
  KEY FINDING: ML-DSA precompile gives negligible ~1.04x speedup vs MAYO's
  1.15-2.21x. Reason: ML-DSA lattice verify (NTT + integer modular) is
  cheap branch-light wasm; dominant gas is fixed per-tx overhead for large
  pk+sig marshalling, not verify compute. Argues AGAINST spending host-fn
  budget on ML-DSA specifically (MAYO's precompile is worth it, ML-DSA's is not).

REMAINING (future sessions):
  - Wire hybrid consensus keys into `junod init` to get measured hybrid
    commit_bytes (expected ~12-14KB vs 2266B classical for N=4 validators).
    Requires either: (a) init generates ML-DSA-44 consensus keys, or
    (b) MsgRotateConsKey F6 rotation post-genesis.
  - D3 full wiring: proto/keyring/CLI/gas via protoc for hybrid account keys.


NOTE TO FUTURE SELF:
The discipline is: ship the safe failure mode first, then iterate. The v30
RetentionWindowHeights = 0 default is the canonical example - unbounded
storage growth is the better failure mode than silently zeroing sparse
delegators. The same shape applies to our LST allowlist (empty by default
at junoclaw launch) and our agent-registry's default reputation (zero, not
implicit-positive).
