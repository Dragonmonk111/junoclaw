[package]
name = "junoclaw-memory"
version = "0.1.0"
edition = "2021"
description = "L1 Memory Index — Merkle-verified memory layer for robot recall"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
bincode = "1"
parking_lot = "0.12"
