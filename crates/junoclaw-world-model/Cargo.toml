[package]
name = "junoclaw-world-model"
version = "0.1.0"
edition = "2021"
description = "L2 World Model — predicts consequences of candidate actions in ~100ms"

[dependencies]
junoclaw-memory = { path = "../junoclaw-memory" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
parking_lot = "0.12"
